#include <iostream>
using namespace std;

const int maxl=15;
const int maxw=25;
int O[maxl], I[maxl], S[maxl];

int main() {
    int l, w; scanf("%d%d", &l, &w);
    for (int i=0; i<l; i++) scanf("%d%d%d", O+i, I+i, S+i);
    int p
}